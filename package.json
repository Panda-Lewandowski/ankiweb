{
  "name": "ankiweb-shell",
  "private": true,
  "devDependencies": { "esbuild": "^0.21.0", "typescript": "^5.4.0" },
  "scripts": {
    "build": "node tools/build_shell.mjs",
    "web:build": "npm --prefix web run build",
    "web:test": "npm --prefix web test"
  }
}
